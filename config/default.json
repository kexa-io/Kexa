{
  "http": [
    {
      "name": "check kexa website",
      "prefix": "ENDPOINTA-",
      "description": "First environment (0) : EndPoint A environment",
      "rules": ["HTTPRules"],
      "METHOD": "GET",
      "URL": "https://www.kexa.io",
      "body": { }
    }
  ],
  "snowflake": [
    {
      "name": "check snowflake",
      "prefix": "A-",
      "description": "Second environment (1) : EndPoint B environment",
      "rules": ["SnowflakeRules"]
    }
  ]
}
